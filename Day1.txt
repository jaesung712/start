GIT
- 버전 관리 시스템의 종류

* 버전관리 : 여러 파일을 하나의 버전으로 묶어 관리하는 것

- 버전관리 시스템의 종류
    1. 클라이언트 - 서버 모델
        - 하나의 중앙 서버로 여러 클라이언트들이 각자 필요한 데이터만 가져와서 작업을 하고 , 다시 중앙 서버로 보내서 통합하는 방식
        - CVS , SVN

    2. 분산 모델
        - 하나의 중앙 서버가 존재 하지만 여러 클라이언트들은 각자의 컴퓨터 저장소에 전체 사본을 가지고 작업하는 방식
        - GIT

    
- GIT의 장점
    1. 동시에 작업하는 사람들과 소스코드를 주고받을 필요가 없음
    2. 같은 파일을 여러명이 동시에 병렬 개발이 가능
    3. 변동 과정을 체계적으로 관리할 수 있고 , 언제든지 지난 시점의 버전으로 되돌릴 수 있음
    4. 인터넷이 연결되지 않은 곳에서도 개발을 진행할 수 있꼬, 중앙 서버의 데이터가 유실되어도 다시 복구할 수 있음.
    
cmder 설치
- 터미널 기본명령어
    1.큰트롤 + L :화면클리어
    2.pwd : 현재 디렉토리 경로를 표시
    3.dir :현재 디렉토리에 존재하는 디렉토리와 파일을 표시
        git 상태-> ls
    4. cd : 다른 디렉토리로 이동
        cd.. : 상위 디렉토리로 이동
        git 상태 -> cd.. 
        cd 디렉토리명 : 해당 디렉토리로 이동


GIT 시작하기

1.git이 설치되어 있는지 확인
    - cmder 에서 git명령어로 확인
'git'은(는) 내부 또는 외부 명령, 실행할 수 있는 프로그램, 또는
배치 파일이 아닙니다.

2.구글에서 'git 설치' 검색 , git설치 
https://git-scm.com/book/ko/v2/%EC%8B%9C%EC%9E%91%ED%95%98%EA%B8%B0-Git-%EC%84%A4%EC%B9%98
3. 왼쪾 좌측 메뉴중 downloads 클릭
4. 자신의 운영체제에 맞는 프로그램을 다운로드
5. 기본설정으로 설치
6. 설치되어있는지 재확인

git 명령어
1. git 로컬 저장소 생성
    git init
    ls -al (숨긴 디렉토리 확인)

2. git에 버전관리할 파일을 선택 ->stage에 올림
    git add index.html

3. 버전을 만들기 ->커밋
    git commit -m

    Author identity unknown

*** Please tell me who you are.

Run

  git config --global user.email "you@example.com"
  git config --global user.name "Your Name"

to set your account's default identity.
Omit --global to set the identity only in this repository.

fatal: unable to auto-detect email address (got '82108@DESKTOP-K1VQNTL.(none)')

해결방법
GIT CONFIG --global
git config --global user.email "사용할 이메일주소"
git config --global user.name "이름"

4.로그 확인 
    git log
    fatal: your current branch 'master' does not have any commits yet     ?

5. 여러파일을함꼐 서낵- >스테이지에 혀재 디렉토리에 있는 모든 파일을 올림
    git add .

6.여러 파일 커밋
    git commit -m "현재 디렉토리에 있는 모든 파일을 커밋"